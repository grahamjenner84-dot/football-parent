# SEO Research System - Setup Report

Generated by `npx tsx scripts/seo/cli/setup.ts`. No live DataForSEO calls
were made to produce this report.

## Result: 21/21 checks passed

| Check | Result |
|---|---|
| Required directories (`seo-data/*`, `scripts/seo/*`) | OK - all present |
| `DATAFORSEO_USERNAME`/`DATAFORSEO_LOGIN` present | OK (present, value not read into this report) |
| `DATAFORSEO_PASSWORD` present | OK (present, value not read into this report) |
| `GOOGLE_SERVICE_ACCOUNT_EMAIL` present | OK |
| `GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY` present | OK |
| `GSC_SITE_URL` present | OK |
| DataForSEO environment defaults safely | OK - resolves to `sandbox` |
| Live calls not silently allowed | OK - `DATAFORSEO_ALLOW_LIVE=false` |
| SQLite schema migrated | OK - schema_version=1, via Node's built-in `node:sqlite` |
| Existing GSC connection reachable | OK - service-account JWT auth succeeded, 71 page rows in a 1-day probe |

## Known deviation from the task brief

The brief specifies env var name `DATAFORSEO_USERNAME`. This repository's
`.env.local` actually has `DATAFORSEO_LOGIN`. The system reads both
(preferring `DATAFORSEO_USERNAME` if it's ever added), so this required no
`.env.local` change and is not a blocker.

## Environment substitution

`better-sqlite3` fails to compile on this machine (Visual Studio is
installed but missing the "Desktop development with C++" workload - a real
`npm install better-sqlite3` was attempted and failed at the `node-gyp`
configure step). Per the task brief's own fallback instruction, the system
uses Node's built-in `node:sqlite` (`DatabaseSync`) instead - zero native
dependencies, ships inside the Node 24 binary already installed here, no
toolchain required.

## Sandbox integration

DataForSEO sandbox authentication was fixed (password rotated in
`.env.local`) and the full sandbox integration test now passes 10/10,
including cache-hit-on-repeat-request and correct sandbox/live tagging -
see `sandbox-test-report.md` for details, including two request-construction
bugs found and fixed against the real sandbox response data.
